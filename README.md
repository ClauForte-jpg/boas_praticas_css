1. Atualizar as classes de acordo com a Metodologia BEM.
Código utiliza BEM. 
produtos (bloco principal)
produtos__item, produtos__imagem, produtos__item-nome, produtos__item-descricao (elementos filhos)
produtos__item--destaque (modificador de estado ou variação)
2. Transcrição do CSS utilizando o sistema de blocos, elementos e/ou modificadores.
O CSS utiliza as classes BEM para aplicar os estilos. 
3. A escolha da abordagem para a transcrição dos estilos é livre (SASS/LESS/CSS puro).
Abordagem escolhida foi CSS Puro.
CSS puro (Vanilla CSS). Essa é uma escolha válida e atende ao requisito. O importante é manter a organização que você já tem.
